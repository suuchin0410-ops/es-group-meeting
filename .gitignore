# Notion tokens
app/config/.notion_token
app/config/.notion_token_es_group

# Claude Code のローカル設定
.claude/settings.local.json

# Python
__pycache__/
*.pyc
.venv/

# Excel の一時ロックファイル
~$*.xlsx

# 議事録・会議資料はリポジトリ管理外（公開リポジトリのため）
# 個人名・グループ間の税務処理の経緯を含むため、Notion とローカルでのみ管理する。
docs/
